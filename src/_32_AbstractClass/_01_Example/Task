
Task:
Basit Bir Abstract Class Kullanımı

Amaç:
Bu görevde, abstract class’ın temel kullanımını öğrenmeyi amaçlıyoruz.
Basit bir abstract class tanımlayarak onu bir sınıfta nasıl extend edebileceğimizi göreceğiz.

Görev Açıklaması:
1. `DocumentBase` adında bir abstract class oluşturun.
    Bu class içinde `print()` adında bir soyut (abstract) metot tanımlayın.
2. `Document` adında bir sınıf oluşturun. Bu sınıf `DocumentBase` abstract class’ını extend etsin.
3. `Document` sınıfında `print()` metodunu özelleştirerek ekrana bir mesaj yazdırın.

İpucu:
- Abstract class içinde hem soyut hem de somut metotlar bulunabilir.
