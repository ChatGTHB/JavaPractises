
Task:
Basit Bir Interface Kullanımı

Amaç:
Bu görevde, interface’in temel kullanımını öğrenmeyi amaçlıyoruz.
Basit bir interface tanımlayarak onu bir sınıfta nasıl implement edebileceğimizi göreceğiz.

Görev Açıklaması:
1. `Printable` adında bir interface oluşturun. Bu interface içinde `print()` metodu tanımlayın.
2. `Document` adında bir sınıf oluşturun. Bu sınıf `Printable` interface’ini implements etsin.
3. `Document` sınıfında `print()` metodunu özelleştirerek ekrana bir mesaj yazdırın.

İpucu:
- Interface içindeki metotların gövdesi olmaz, implement eden sınıfta tanımlanır.
