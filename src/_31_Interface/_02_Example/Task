
Task:
Birden Fazla Interface Kullanımı

Amaç:
Bu görevde, bir sınıfın birden fazla interface'i nasıl implement edebileceğini öğrenmek amaçlanmaktadır.

Görev Açıklaması:
1. `IReadable` ve `IWritable` adında iki interface oluşturun.
`IReadable` interface içinde `read()`, `IWritable` interface içinde `write()` metodunu tanımlayın.
2. `TextFile` adında bir sınıf oluşturun. Bu sınıf hem `IReadable` hem de `IWritable` interface’ini implements etsin.
3. `TextFile` sınıfında `read()` ve `write()` metotlarını tanımlayın ve ekrana bir mesaj yazdırın.

İpucu:
- Bir sınıf aynı anda birden fazla interface'i implements edebilir.

