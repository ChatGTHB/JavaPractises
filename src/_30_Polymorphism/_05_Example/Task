Task: Araçlar Hiyerarşisi

Amaç:
Bu görevin amacı, polymorphism kullanarak farklı araç türlerinin motor çalışma davranışlarını modellemektir.

Görev Açıklaması:
1. `Vehicle` adında bir sınıf oluşturun. Bu sınıf, `startEngine()` ve `stopEngine()` metotlarını içersin.
2. `Car` ve `Motorcycle` adında iki alt sınıf oluşturun.
    Bu sınıflar `Vehicle` sınıfından miras alsın ve `startEngine()` metodunu özelleştirsin (override).
3. `Main` sınıfında hem `Car` hem de `Motorcycle` nesneleri oluşturup, motor çalışma davranışlarını simüle edin.

İpucu:
- Polymorphism ile alt sınıf nesneleri oluşturup üst sınıf referansı kullanarak motor çalışma davranışlarını gösterin.
