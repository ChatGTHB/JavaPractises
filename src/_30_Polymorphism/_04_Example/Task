Task: Banka Hesap Yönetimi

Amaç:
Bu görevin amacı, polymorphism kullanarak farklı hesap türlerinin işlem kurallarını göstermektir.

Görev Açıklaması:
1. `BankAccount` adında bir sınıf oluşturun. Bu sınıf, `withdraw()` ve `deposit()` metotlarını içersin.
2. `CheckingAccount` ve `SavingsAccount` adında iki alt sınıf oluşturun.
    Bu sınıflar `BankAccount` sınıfından miras alsın ve `withdraw()` metodunu özelleştirsin (override).
3. `Main` sınıfında hem `CheckingAccount` hem de `SavingsAccount` nesneleri oluşturup, hesap işlemlerini simüle edin.

İpucu:
- Polymorphism ile alt sınıf nesneleri oluşturup üst sınıf referansı kullanarak işlem kurallarını uygulayın.
